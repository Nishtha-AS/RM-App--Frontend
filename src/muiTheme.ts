// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e88e5', // You can customize this color
    },
    secondary: {
      main: '#f50057',
    },
  },
});


export default theme;
